help_en: 'convert the conceptual model into a physical model for Oracle'
help_fr: 'convertit le modèle conceptuel en un modèle physique pour Oracle'
fr_examples:
  - order: 1
    example: 'oracle'
    explanation: 'version de base'
  - order: 4
    example: 'oracle:b'
    explanation: 'avec _boilerplate_'
parent: 'sql'
stem_suffix: '_ddl_oracle'
transform_datatype:
  - order: 100
    comment: '(Meant to replace the parent''s transformation)'
    search: '^\s*$'
    replace: 'VARCHAR2(42)'
  - order: 200
    comment: '(Meant to replace the parent''s transformation)'
    search: 'UNSIGNED_INT_PLACEHOLDER'
    replace: 'NUMBER(1) UNSIGNED'
  - order: 300
    comment: '(Meant to replace the parent''s transformation)'
    search: 'BOOLEAN_PLACEHOLDER'
    replace: 'NUMBER(1) DEFAULT 0'
